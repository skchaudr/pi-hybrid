[workspace]
members = [
    "rust-core",
    "ts-bridge",
    "py-extensions",
]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
strip = true
